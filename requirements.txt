# Jarvis Mark II — Core Dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
edge-tts>=6.0
python-multipart>=0.0.9
httpx>=0.27.0
pyyaml>=6.0
pydantic>=2.0.0
aiofiles>=23.0.0

# Web tools
duckduckgo_search>=8.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
readability-lxml>=0.8.0
aiohttp>=3.9.0

# Optional: Desktop native window (pywebview — for legacy --server --desktop mode)
# pywebview>=5.0.0

# Optional: Speech-to-text
faster-whisper>=1.0.0
pydub>=0.25.0

# Optional: Vector memory
# chromadb>=0.5.0
# fastembed>=0.3.0

# Optional: Email integration
# httpx[socks]
# caldav>=1.3.0

# Optional: Image processing
# Pillow>=10.0.0
# rembg>=0.1.0
